# 도메인 간 의존성 맵

> `/claude-sdd:sdd-spec`이 도메인 분석 결과로 자동 생성

## 의존성 그래프

![의존성 그래프](diagrams/cross-domain-dependency.png)

> *이 다이어그램은 `sdd-generate-diagram.py`가 스펙에서 자동 생성합니다. 직접 편집하지 마세요.*
>
> 화살표 방향: A → B 는 "A가 B에 의존"을 의미합니다.

## 도메인별 의존성 상세

### {{DOMAIN_NAME}} (`{{DOMAIN_ID}}`)

| 방향 | 대상 도메인 | 의존 유형 | 설명 |
|------|-------------|-----------|------|
| 의존 대상 (depends on) | {{TARGET_DOMAIN}} | {{DEPENDENCY_TYPE}} | {{DEPENDENCY_DESCRIPTION}} |
| 의존받는 곳 (depended by) | {{SOURCE_DOMAIN}} | {{DEPENDENCY_TYPE}} | {{DEPENDENCY_DESCRIPTION}} |

### {{DOMAIN_NAME}} (`{{DOMAIN_ID}}`)

| 방향 | 대상 도메인 | 의존 유형 | 설명 |
|------|-------------|-----------|------|
| 의존 대상 (depends on) | {{TARGET_DOMAIN}} | {{DEPENDENCY_TYPE}} | {{DEPENDENCY_DESCRIPTION}} |
| 의존받는 곳 (depended by) | {{SOURCE_DOMAIN}} | {{DEPENDENCY_TYPE}} | {{DEPENDENCY_DESCRIPTION}} |

## 공유 엔티티

| 엔티티 | 소유 도메인 | 참조 도메인 | 참조 유형 |
|--------|-------------|-------------|-----------|
| {{ENTITY_NAME}} | {{OWNER_DOMAIN}} | {{REFERENCING_DOMAINS}} | {{REFERENCE_TYPE}} |

> **참조 유형 범례**:
> - `FK` — 외래 키 참조 (직접 DB 관계)
> - `ID 참조` — ID만 저장하고 API로 조회
> - `복제` — 필요한 필드를 도메인 내부에 복제하여 사용
> - `이벤트` — 이벤트 기반 동기화

---

*생성 원본: 도메인 아키텍처 분석 | 관련 문서: docs/specs/02-architecture.md*
